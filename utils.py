# utils.py

import random

def get_indicator_data(symbol, interval):
    # Simülasyon yerine burada gerçek analiz hesaplamaları yapılacak
    # Örneğin: TradingView verisi + RSI Swing & RMI analizi

    # Bu satır sadece örnek içindir, testten sonra kaldırılmalı:
    signal = random.choice(["AL", "SAT", "BEKLE"])
    return signal, signal
